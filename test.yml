---
-  name: copy to remote server
   hosts: sp-member
   gather_facts: false
   tasks:
     - name: server stop
       shell: pid=`ps -ef | grep sp-0.0.1-SNAPSHOT.jar | grep -v 'grep' | awk '{print $2}'`
         echo $pid
         kill -9 $pid
         rm -rf /home/ubuntu/app/sp-0.0.1-SNAPSHOT.jar
#     - name: store jar file to remote server
#       copy:
#         src: /var/lib/jenkins/workspace/test/build/libs/sp-0.0.1-SNAPSHOT.jar
#         dest: /home/ubuntu/app
#     - name: server startup
#       shell: nohup java -jar /home/ubuntu/app/sp-0.0.1-SNAPSHOT.jar
